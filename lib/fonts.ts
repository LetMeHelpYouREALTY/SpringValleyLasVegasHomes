import { Great_Vibes } from "next/font/google";

/** Script signature for hero lockups — apply `greatVibes.className` on the name. */
export const greatVibes = Great_Vibes({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-great-vibes",
  display: "swap",
});
